Deleted:
  - obligatory parenthesis and no more ε in funcall args
  - removed the ε from <funcall args>
  - <expr> = <expr>: better compatibility

Bugs:
  - test lib.pl does not work
  - Add functions (number of parameters) to the symbol table, and auto fill the last argument to substr, and concat the arguments of print
  - Replace false by undef
  - grammar's TODO
  - test post cond, unless
  - Test the example from the UV
